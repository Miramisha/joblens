# JobLens: учебный ML-эксперимент

Данные искусственно составлены для разработки. Это не оценка на реальных вакансиях.

Обучение: 60 примеров. Проверка: 24 примеров. Версия: `context-nb-v1`.

| Метод | Accuracy | Macro F1 |
|---|---:|---:|
| Все упоминания обязательны | 0.333 | 0.167 |
| Naive Bayes: слова и пары слов | 0.625 | 0.584 |

## Ошибки

- test-required-02: «Ждём кандидата, который уверенно применяет Docker в коммерческих проектах.» — ожидалось `required`, получено `not_required`.
- test-required-03: «Без практических навыков TypeScript выполнять задачи не получится.» — ожидалось `required`, получено `not_required`.
- test-required-04: «При найме проверяем обязательное владение Python.» — ожидалось `required`, получено `optional`.
- test-required-06: «Вакансия требует глубоких знаний Go.» — ожидалось `required`, получено `not_required`.
- test-required-07: «Умение пользоваться Redis — необходимое условие.» — ожидалось `required`, получено `optional`.
- test-required-08: «Основная обязанность: поддерживать сервисы с использованием Kubernetes.» — ожидалось `required`, получено `optional`.
- test-optional-03: «Знание FastAPI даст преимущество, обязательным не считаем.» — ожидалось `optional`, получено `not_required`.
- test-optional-07: «Навык PyTorch пригодится в перспективе, сейчас он необязателен.» — ожидалось `optional`, получено `not_required`.
- test-optional-08: «Пожелание команды — опыт Node.js, обсудим и кандидатов без него.» — ожидалось `optional`, получено `required`.

## Ограничения

Один автор и сходный стиль обучающих и проверочных примеров могут завышать результат. Классифицируется один отмеченный навык в коротком контексте. Нет нейтрального класса, контекста нескольких противоречивых упоминаний, OCR или исправления опечаток. Оценки не калиброваны. До подключения к продукту нужна независимая разметка реальных данных.

Проверочная выборка не используется для словаря признаков или обучения. Повторная настройка по её ошибкам потребует новой закрытой выборки.

SHA-256 набора данных: `5c33a904544bc848a2213d47c190f5ef426fe162d118e0665b2f77a835f323b6`.
